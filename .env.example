# Supabase (optionnel — l'app fonctionne avec des données mock sans ces vars)
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Authentification admin simple (sans Supabase)
# Ajoutez ces valeurs dans votre .env.local (jamais dans le code)
ADMIN_USERNAME=
ADMIN_PASSWORD=
